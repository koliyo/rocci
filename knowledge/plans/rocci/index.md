# Rocci

Templates, handlers, runtime, desktop inspector, component generation, and falling-block.

* [Implement directory-as-app standalone entry](application-structure-and-entry.md) - A directory is a bag of modules with at most one process `@init`; `rocci run <dir>` when the entry is unique; optional `rocci.toml` `[app].entry`; keep `rocci run File.rocci`. Do not reuse `apps.toml`. Research: [application structure and run entry](/research/rocci/application-structure-and-entry.md). Exploratory; implementation also landed on `application-structure-and-entry` (PR 90).
* [SSE patch-target tests](sse-patch-target-tests.md) - Wire-format rule: every `datastar-patch-elements` event has a selector or an `id` on each top-level element. Lowering still emits the style sibling; optional ignored `GET /sse` smoke. No browser Datastar in the default suite. Exploratory; no phase started. Research: [untagged style siblings](/research/rocci/sse-patch-style-targets.md).
* [Link a real Rocci app into the WASI 0.3 HTTP module](wasi-http-03-app.md) - Follow-on: `--http-module` still copies hello-web stubs. Use sibling `../roc-basic-webserver` for a wasm32 target; Phase 0 recorded that `--no-link` is absent and `roc_app_llvm_wasm32_speed.o` already has the three `roc_*_for_host` names on both nightlies. Link that object so Counter (then live-counter) is what `wasmtime serve` runs. Do not change `--host wasm` or `rocci run`. Exploratory; Phase 0 recorded on `basic-webserver-wasi`. Parent component: [0.3 component](wasi-http-03-component.md). Research: [WASI HTTP gaps](/research/rocci/basic-webserver-wasi.md).
* [WASI 0.3 HTTP component for wasmtime serve](wasi-http-03-component.md) - Research option 1 shipped experimental: Rust is the `wasi:http/service` component; Roc stays the 0.16 C-ABI object. `rocci build --http-module` writes bytes `wasmtime serve -Sp3 -Scli` accepts. Remaining work is the [app link](wasi-http-03-app.md). Do not change `--host wasm` or `rocci run`. Exploratory; Phases 0–8 on `wasi-http-03-component`; not logged complete until CI and Knowledge succeed. Parent embedder: [WASI HTTP adapter](basic-webserver-wasi.md). Research: [WASI HTTP gaps](/research/rocci/basic-webserver-wasi.md).
* [WASI HTTP adapter host for basic-webserver apps](basic-webserver-wasi.md) - New `rocci-wasi-http` WASI 0.3 `wasi:http/service` adapter linking the 0.16 Roc ABI. Yield around Roc (SSE Wait as adapter clocks); do not make Roc async or change `--host wasm`. Exploratory; Phases 0–6 implemented on `basic-webserver-wasi` (native embedder). Portable `wasmtime serve` and `--http-module` component emit: [0.3 component](wasi-http-03-component.md). Research: [WASI HTTP gaps](/research/rocci/basic-webserver-wasi.md).
* [Implement @test and rocci test](rocci-test-syntax.md) - Root-only `@test name = boolExpr` with optional `{fixture: ident}`, lower to Roc `expect` outside `wrap_type_module`, add `rocci test`, then skills, docs, AllSyntax, and example fixtures. Exploratory; Phases 0–7 implemented on `rocci-test-syntax`; not logged complete until CI and Knowledge succeed. Research: [Rocci @test syntax](/research/rocci/rocci-test-syntax.md).
* [Pin Roc 2026-08-23 and emit type-position defaults](roc-nightly-defaults.md) - Pin `nightly-2026-08-23-fb208ba`. Lower authored `??` to a Roc type annotation and a stripped pattern; stop call-site fill. Exploratory. Research: [Roc nightly record defaults](/research/rocci/roc-nightly-record-defaults.md).
* [Implement verb-first handler declarations](verb-first-handler-declarations.md) - Mandatory `@method:role` routes, explicit document/fragment/command metadata, GET HTML fragments, representation-free commands, path-addressed GET-live routes, and low-level Roc patch-signals. Supersedes the earlier handler-boundary plan as the implementation sequence while retaining its UI boundary. Phases 0–6 implemented on `verb-first-handler-declarations`; Phase 7 documentation and release gates are in this revision and are not logged complete until CI and Knowledge succeed. Research: [verb-first handler declarations](/research/rocci/verb-first-handler-declarations.md).
* [Bound Rocci handlers to server-rendered UI](handler-ui-boundary.md) - Earlier follow-up plan with a Phase 0 `@patch` versus `@fragment` gate, semantic route metadata, one-shot GET HTML fragments, representation-free commands, and low-level Roc patch-signals. Its proposed implementation sequence is superseded by [verb-first handlers](verb-first-handler-declarations.md); no phase started.
* [Semantic view, patch, command, and live handlers](action-handler-syntax.md) - Historical plan for the earlier clean cut from `@on` to `@view`, `@patch`, `@command`, and `@live`, including generated command JSON. Superseded as the current language by [verb-first handlers](verb-first-handler-declarations.md). Research: [bounded handler syntax](/research/rocci/action-handler-syntax.md).
* [Implement path-addressed live routes](path-addressed-live-streams.md) - Plural `@get:live(path)` declarations, module-local singleton injection, explicit multi-stream subscriptions, primary+sibling app binding, app-level collision errors, coarse-stream examples, and connection/poll cost evidence. Implemented together with the verb-first cutover on `verb-first-handler-declarations`; Phase 7 documentation and release gates are in this revision and are not logged complete until CI and Knowledge succeed. Research: [path-addressed live streams](/research/rocci/path-addressed-live-streams.md).
* [Generated CQRS streams and JSON command responses](datastar-cqrs-action-responses.md) - Opt-in `@live` so Rocci generates `/sse` and `data-init`; `json` POSTs return 204 vs JSON. Keep the first-app counter one-shot; add `live-counter`; convert the hybrid island. Exploratory; Phases 1–5 implemented on `datastar-cqrs-action-responses`, not CI-complete. Research: [Datastar SSE vs fan-out](/research/rocci/datastar-cqrs-action-responses.md).
* [Standalone falling-block authoring example](standalone-falling-block.md) - Nested standalone Rocci Blocks: server-owned gravity, HTML board, `@post:command` plus `@get:live`, app-root `backend/` and `ui/` discovery. Retires the custom arena and `/play/blocks/` origin exception. Exploratory; Phases 1–6 implemented on `standalone-falling-block`. Not CI-complete.
* [Multiplayer falling-block demonstrator on rocci.dev](multiplayer-falling-block-demonstrator.md) - Historical custom-arena design: one same-origin public arena for up to eight active players and a measured target of fifty spectators. Browser code owned falling-piece motion; Roc validated lock commits. Superseded as the product shape by [standalone falling-block](standalone-falling-block.md). Exploratory; Phases 0–6 described that custom arena.
* [Full Rocci and Rocdown language tooling](language-server.md) - Proposed region-aware editor tooling with shared token spans and product-owned server composition under the boundary refactor.
* [Embedded Roc LSP parity with zed-roc](embedded-roc-lsp-parity.md) - Optional `roc experimental-lsp` child on generated modules so executable Roc inside `.rocci` and `.rocdown` gets mapped hover, diagnostics, completion, definition, and references. Exploratory; no phase started. Umbrella: [language tooling](language-server.md).
* [Ungrammar AST codegen for Rocci and Rocdown](ungram-ast.md) - Phased owned-struct generation from per-language ungrams via a shared `rocci-ungram` CLI; scanners and parsers stay hand-written. Exploratory; Phases 1–5 implemented on `ungram-ast-implementation`, not CI-complete.
* [Ungram follow-on backends after owned-struct codegen](ungram-follow-ons.md) - Freeze inspect tags and generate exhaustive `format_ast` walkers, add `NodeKind` highlighter coverage, generate `MdNode` from a Markdown ungram, and emit a public tree appendix. Do not generate `SyntaxKind`, highlighters, or a CST. Exploratory; no phase started.
* [First-party Rocci chrome library and generation host](rocci-component-generation.md) - Extract demonstrated outline/nav/breadcrumb chrome into base Rocci, host Roc through a cached native subprocess and Wasmtime, and persist both generated Roc and compiled artifacts.
* [Extended preview-window inspector](preview-inspector.md) - Dock, tabs, Source DX, and runtime console as originally specified. Those shells are in the tree; remaining investigation and repair (scroll, dock chrome, OKF routes, highlighting) is [investigate and repair the preview inspector](preview-inspector-repair.md).
* [Investigate and repair the preview inspector](preview-inspector-repair.md) - Finish the product/window matrix in wry, then fix Source scroll (`@scope` vs panel root), overlay R/B covering tabs, theme `100vh` inset, missing OKF inspect routes, and `tok-*` highlighting. Exploratory; Phases 1–7 implemented in this revision, not CI-complete.
* [Preview inspector source views](inspector-source-views.md) - Inspect JSON and a Dev-panel dropdown for original source, AST, generated Roc, and generated HTML. Artifact fill shipped; remaining UX is the [inspector repair](preview-inspector-repair.md) plan.
* [Runtime console without a component log API](inspector-console-scope.md) - Keep Console as host runtime lines; feed `rocci run` Roc stderr into sibling `InspectorServer`. Do not add `@component` logging. Exploratory; Phases 1–3 implemented in this revision, not CI-complete. Research: [console scope](/research/rocci/inspector-console-scope.md).
* [Live reload follow-ons after the preview chrome toggle](live-reload-follow-ons.md) - Native View menu check item, shared `--no-live-reload` CLI flag, and a `--no-window` browser pause. Exploratory; Phases 1–3 implemented in this revision, not CI-complete.
* [Deferred live-reload controls after the three follow-ons](live-reload-deferred.md) - Stop the file watcher, per-route reload policy, and inspector-only pause. Exploratory; no phase started.
