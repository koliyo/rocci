@page {
    meta: {
        title: "Rocs site configuration",
        description: "Configure rocci.dev-style documentation metadata, output, assets, and curated navigation in rocs.toml.",
    },
}

# Rocs site configuration

Place `rocs.toml` at the documentation content root when you need site metadata,
hashed assets, or curated navigation. The file is optional: a directory of
`.rocdown` pages, or a single `.rocdown` file whose parent is the content root,
is enough for `rocs build`, `rocs check`, and `rocs run`.

```sh
cargo run -p rocs-cli -- build report.rocdown --output dist
cargo run -p rocs-cli -- run report.rocdown
```

## Example

```toml
[site]
title = "Rocci"
description = "Roc-native interface tools"
base_url = "https://rocci.dev"
language = "en"
repository = "https://github.com/koliyo/rocci"
social_image = "/assets/og.png"
subtitle = "Roc-native interface tools"
footer = "Rocci is experimental software."

[build]
output = "../dist/rocci.dev"
assets = "assets"

[[nav]]
label = "Start here"
items = ["index", "getting-started/quickstart"]

[[nav]]
label = "Reference"
items = ["reference/rocci", "reference/rocdown"]
```

Unknown keys are errors.

## Snippets and examples

Optional snippet roots are resolved relative to the content root. They still
cannot escape the repository parent, and include paths cannot contain `..`,
NUL, or absolute locations.

```toml
[snippets]
roots = ["snippets", "../examples"]

[examples]
timeout_ms = 30000
allow_network = false
```

`@docs include` reads a file as nested Rocdown (`.rocdown`, no `@page`) or as a
code literal. Named regions use `docs-region: NAME` / `docs-region-end: NAME`
markers. Line ranges emit warning `RD2504`. Cycles are `RD2505`. Included files
are watch inputs for `rocs run`.

`rocs check` validates components and includes without Roc. Untested
`@docs example` records warn (`RD2601`). Example commands never run during
`rocs build` or `rocs check`.

```sh
cargo run -p rocs-cli -- test docs
cargo run -p rocs-cli -- test docs --update
```

`rocs test` runs declared `test` argument lists with cwd at the content root, a
timeout, no extra environment, and no shell expansion. `--update` rewrites
golden files. Failures are diagnostics (`RD2603`); `dist/` is not written.
Fenced code is never executed. Default CSP stays `script-src 'none'`; tab
panels are stacked sections, not a JavaScript tablist.

## Site fields

| Field | Default | Meaning |
| --- | --- | --- |
| `title` | `Documentation` | Header, metadata, and generated index title |
| `description` | Empty | Default page and agent-index description |
| `base_url` | Empty | Canonical, sitemap, and absolute discovery URLs |
| `language` | `en` | Document language |
| `repository` | Empty | Source link in the header |
| `social_image` | Empty | Site-relative Open Graph image path |
| `subtitle` | Empty | Short header tagline beside the brand |
| `footer` | Empty | Article footer text |
| `csp` | Strict static default | Content-Security-Policy for generated HTML |

`base_url` must use HTTP or HTTPS. A trailing slash is normalized away.
Empty `subtitle` and `footer` omit those chrome pieces. Leave `csp` empty to use
the default policy: no scripts, no `unsafe-eval` or `unsafe-inline`, styles and
images from `'self'`.

## Build fields

`output` is resolved relative to the content root unless `--output` overrides
it. `assets` names a directory whose files are content-hashed into `/assets/`
in the published tree (`og.png` becomes `og.<hash>.png`). Markdown and `@img`
URLs under that tree are rewritten whether they are authored as
`/assets/foo.png` or `assets/foo.png`. `social_image` is rewritten the same way.

Local image paths resolve against the source file directory. Missing files fail
`rocs check` with `RD2103`. Validated page and heading links are rewritten to
emitted routes after catalog resolve, so a passing check implies a usable href.

Rocs writes into a staging directory and atomically replaces the output only
after a successful build.

The first-party theme CSS is extracted once, hashed, and linked from every
page. Generated HTML declares the CSP with a `<meta>` tag.

## Navigation

Each navigation item is a stable page ID derived from the content-root-relative
path without `.rocdown`:

| Source | Page ID | Route |
| --- | --- | --- |
| `index.rocdown` | `index` | `/` |
| `guides/index.rocdown` | `guides/index` | `/guides/` |
| `guides/build.rocdown` | `guides/build` | `/guides/build/` |

Navigation order is explicit. Unknown IDs and duplicate items fail the build.
Published pages may remain unlisted when that is intentional.

## Generated artifacts

In addition to page HTML, a configured build emits hashed `/assets/` files, a
themed `404.html`, alias redirect HTML, and `llms.txt`. When `base_url` is set,
it also emits `sitemap.xml` and `robots.txt`.

`rocs inspect artifacts` lists the planned output tree, including hashed asset
paths, without invoking Roc.

Build this site's content from the repository root with:

```sh
cargo run -p rocs-cli -- build docs
```

Preview with watch and live reload:

```sh
cargo run -p rocs-cli -- run docs
```

`rocs run` writes to a temp directory by default so it does not replace
`build.output`. Pass `--output` to keep the preview files. `--no-window` serves
in the browser only. Static content reloads after a successful rebuild; dynamic
islands are not part of this preview path.

## OKF knowledge collections

`rocs knowledge` is isolated from ordinary documentation discovery. It reads
only the explicitly supplied OKF bundle (default `knowledge/`) and treats every
non-reserved `.md` file as inert Markdown with YAML frontmatter. `index.md` and
`log.md` retain their OKF meanings; `.rocdown` files and `@page` metadata are
not accepted as canonical knowledge records.

Preview a knowledge collection with watch and live reload:

```sh
cargo run -p rocs-cli -- knowledge run knowledge
```

The preview uses the standard Rocs theme and a temporary output directory by
default. It maps OKF Markdown links to generated routes, serves linked bundle
resources, derives navigation sections from collection `index.md` files, and
retains the last successful site when a later edit fails validation.
The left navigation keeps every top-level collection visible and expands only
the collection containing the current page.

The body parser shares Rocdown's static Markdown AST and `rd-*` rendering
classes, with keyed footnotes enabled. Rocdown declarations, raw HTML, and
wikilinks are disabled for portability and execution safety.

Knowledge builds also emit `catalog.json`, heading-chunked `search.json`,
`llms.txt`, and `validation.json`. Catalog inspection and search can filter by
type, tag, lifecycle status, authority, derived trust tier, and staleness. The
generated site and indexes are local/repository-visible artifacts; no public
deployment or verbatim archive publication is configured.

Measure the checked-in lexical retrieval baseline with:

```sh
cargo run -p rocs-cli -- knowledge benchmark knowledge
```

The versioned question set records expected concepts plus lifecycle and
authority metadata. The command reports top-five hit rate and mean reciprocal
rank as JSON and exits unsuccessfully when the configured hit-rate threshold is
not met. This baseline is intentionally lexical; it should be measured before
adding embeddings or a database service.
