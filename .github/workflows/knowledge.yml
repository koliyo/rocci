name: Knowledge

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    name: Validate Knowledge OKF Bundle
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Test knowledge implementation
        run: |
          cargo test -p rocci-rocdown okf --lib
          cargo test -p rocci-rocdown-cli
      - name: Validate profile, graph, provenance, freshness, and source drift
        run: |
          mkdir -p target/knowledge-ci
          cargo run -q -p rocci-rocdown-cli -- knowledge check knowledge --profile rocci --format json > target/knowledge-ci/validation.json
          cargo run -q -p rocci-rocdown-cli -- knowledge inspect --profile rocci graph knowledge > target/knowledge-ci/graph.json
          cargo run -q -p rocci-rocdown-cli -- knowledge benchmark knowledge > target/knowledge-ci/retrieval.json
      - name: Verify deterministic publication artifacts
        run: |
          cargo run -q -p rocci-rocdown-cli -- knowledge build knowledge --output target/knowledge-ci/build-a
          cargo run -q -p rocci-rocdown-cli -- knowledge build knowledge --output target/knowledge-ci/build-b
          diff -qr target/knowledge-ci/build-a target/knowledge-ci/build-b
