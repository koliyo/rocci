# Styling

Local run notes. Published tutorial: https://rocci.dev/examples/styling/

```sh
cargo run -q -p rocci-cli -- run examples/rocci/standalone/styling/Styling.rocci
```

```sh
curl -s http://127.0.0.1:8000/ | grep -E 'data-rocci-css|@scope'
```
