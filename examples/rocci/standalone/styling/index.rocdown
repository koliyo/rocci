@page {
    layout: "docs",
    meta: {
        title: "Styling",
        description: "File-level and component-scoped CSS",
    },
}

# Styling

See [Templates](/docs/templates/) for components and colocated CSS.

A standalone page that shows colocated `@css`: file-level document chrome,
component-scoped rules, default component params, and Vue-style isolation.
There is no database. v1 injects a `<style>` tag into `<head>` and wraps rules
in `@scope ([data-rocci-css~="id"])`. Put document chrome on `body`, not `html`.

Docs-only on the public origin. See [source](/examples/styling/source/).

## Run locally

```sh
cargo run -q -p rocci-cli -- run examples/rocci/standalone/styling/Styling.rocci
```

## Smoke

```sh
curl -s http://127.0.0.1:8000/health
curl -s http://127.0.0.1:8000/ | grep -E 'data-rocci-css|hello-list|feature-card|@scope'
```
