@page {
    route: "/pair/",
    meta: {
        title: "Pair",
        description: "Two island instances with distinct host ids",
    },
}

@component
AlphaSlot = |{ open }| {
    <div id="slot-alpha">
        @if open {
            <p>Alpha open</p>
        } @else {
            <button type="button" data-on:click=@post("/actions/alpha/show")>
                Open alpha
            </button>
        }
    </div>
}

@component
BetaSlot = |{ open }| {
    <div id="slot-beta">
        @if open {
            <p>Beta open</p>
        } @else {
            <button type="button" data-on:click=@post("/actions/beta/show")>
                Open beta
            </button>
        }
    </div>
}

@post:fragment("/actions/alpha/show") = |_, _request| {
    alphaSlot({ open: True })
}

@post:fragment("/actions/beta/show") = |_, _request| {
    betaSlot({ open: True })
}

# Pair

Two hosts. Each mutation returns only its own element id.

@render AlphaSlot({ open: False })

@render BetaSlot({ open: False })
