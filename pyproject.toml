[project]
name = "rocci"
version = "0.0.0"
description = "Rocci repository operator workspace."
requires-python = ">=3.12"
dependencies = ["rocci-ops"]

[tool.uv]
package = false

[tool.uv.workspace]
members = ["tools/rocci-ops"]

[tool.uv.sources]
rocci-ops = { workspace = true }
