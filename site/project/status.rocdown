@page {
    layout: "plain",
    meta: {
        title: "Project status",
        description: "See what works today, what remains deliberately limited, and which architecture decisions are stable.",
    },
}

# Project status

Rocci is an active experiment with working template and document compilers, examples, editor support, local runtimes, hybrid site delivery, and local macOS bundling. This page describes the shipped repository snapshot, not a stability promise.

## Working today

- `.rocci` parsing and deterministic lowering to Roc.
- Components, fixtures, structural directives, scoped CSS, and Datastar actions.
- Standalone `@context`, `@init`, and verb-first `@method:role` applications.
- `.rocdown` Markdown, page metadata, Roc and Rocci regions, links, themes, and preview.
- Build, inspect, AST, run, view, and browse commands.
- VS Code and Zed language registration plus LSP diagnostics and navigation.
- Tao/Wry embedded preview windows.
- Local ad-hoc-signed macOS application bundles.
- Rocdown site generator with nested content, curated navigation, static HTML, aliases, sitemap, robots, and `llms.txt`.
- Custom project `.rocci` themes, layout dispatch, and metadata propagation.
- Hybrid Rocdown builds with CDN snapshots, colocated handlers, and a sibling island service.

## Deliberate current limitations

- The page finder searches page titles and paths, not full article text.
- The reserved `@island` syntax and client-owned dynamic island splicing are not implemented; hybrid sites use existing server handlers.
- The desktop host manages one main window.
- Windows and Linux installers are not implemented.
- Production signing, notarization, updates, tray behavior, and deep links are absent.
- Native capabilities such as dialogs, filesystem access, and notifications are planned.
- Full macOS, Windows, Linux X11, and Linux Wayland validation is not in CI.

## Stable architectural direction

The project intends to keep these boundaries:

1. Rocci and Rocdown are explicit source syntaxes that lower to ordinary Roc or semantic content data.
2. HTML and local HTTP remain the interface between backend and browser or webview.
3. Datastar is browser transport, not the owner of durable domain state; it is pinned per application and never auto-updated by `run`.
4. Rocdown keeps its catalog and static prose processing in Rust while authoring site chrome in Rocci.
5. Dynamic behavior remains opt-in and identifiable in source.

## Before adopting

Read [What is Rocci?](/docs/start/what-is-rocci/), run the [examples](/examples/), and review the [roadmap](/project/roadmap/). Platform details live in [Runtime compatibility](/docs/reference/compatibility/).
