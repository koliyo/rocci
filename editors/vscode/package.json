{
  "name": "rocci",
  "displayName": "Rocci",
  "description": "Rocci template language support for VS Code",
  "repository": {
    "type": "git",
    "url": "https://github.com/koliyo/rocci"
  },
  "license": "MIT",
  "publisher": "koliyo",
  "version": "0.1.0",
  "engines": {
    "vscode": "^1.128.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "main": "./out/main.js",
  "scripts": {
    "vscode:package": "vsce package --no-yarn",
    "vscode:prepublish": "npm run esbuild-base -- --minify",
    "esbuild-base": "esbuild ./src/extension.ts --bundle --outfile=out/main.js --external:vscode --format=cjs --platform=node",
    "esbuild": "npm run esbuild-base -- --sourcemap",
    "esbuild-watch": "npm run esbuild-base -- --sourcemap --watch",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts",
    "test:unit": "mocha --ui tdd --timeout 5000 \"out/test/unit/**/*.js\"",
    "test": "npm run compile && npm run esbuild && npm run test:unit && node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/glob": "^9.0.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.19.43",
    "@types/vscode": "^1.128.0",
    "@typescript-eslint/eslint-plugin": "^8.68.0",
    "@typescript-eslint/parser": "^8.68.0",
    "@vscode/test-electron": "^3.1.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.28.2",
    "eslint": "^8.57.1",
    "glob": "^13.0.6",
    "mocha": "^11.8.0",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "vscode-languageclient": "^8.1.0"
  },
  "overrides": {
    "serialize-javascript": "7.0.5"
  },
  "contributes": {
    "configurationDefaults": {
      "[rocci]": {
        "editor.semanticHighlighting.enabled": true
      },
      "[rocdown]": {
        "editor.semanticHighlighting.enabled": true
      }
    },
    "languages": [
      {
        "id": "rocci",
        "aliases": [
          "Rocci"
        ],
        "extensions": [
          ".rocci"
        ],
        "icon": {
          "light": "./icons/rocci-file-light.svg",
          "dark": "./icons/rocci-file.svg"
        },
        "configuration": "./language-configuration.json"
      },
      {
        "id": "rocdown",
        "aliases": [
          "Rocdown"
        ],
        "extensions": [
          ".rocdown"
        ],
        "icon": {
          "light": "./icons/rocci-file-light.svg",
          "dark": "./icons/rocci-file.svg"
        },
        "configuration": "./rocdown-language-configuration.json"
      }
    ],
    "commands": [
      {
        "command": "rocci.restartLspServer",
        "title": "Rocci: Restart LSP server"
      },
      {
        "command": "rocci.preview",
        "title": "Rocci: Preview",
        "icon": "$(open-preview)"
      },
      {
        "command": "rocci.reloadPreview",
        "title": "Rocci: Reload Preview",
        "icon": "$(refresh)",
        "enablement": "rocci.preview.active"
      },
      {
        "command": "rocci.stopPreview",
        "title": "Rocci: Stop Preview",
        "icon": "$(stop)",
        "enablement": "rocci.preview.active"
      },
      {
        "command": "rocci.updateTools",
        "title": "Rocci: Update tools"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "rocci.preview",
          "when": "editorLangId == rocci || editorLangId == rocdown",
          "group": "navigation"
        },
        {
          "command": "rocci.reloadPreview",
          "when": "rocci.preview.active && (editorLangId == rocci || editorLangId == rocdown)",
          "group": "navigation"
        }
      ],
      "editor/title/run": [
        {
          "command": "rocci.preview",
          "when": "editorLangId == rocci || editorLangId == rocdown"
        }
      ]
    },
    "configuration": {
      "title": "Rocci",
      "properties": {
        "rocci.lsp.serverPath": {
          "type": "string",
          "default": "",
          "description": "Absolute path to rocci-language-server. Empty uses a debug build, a verified GitHub-release extract, or PATH."
        },
        "rocci.roc.path": {
          "type": "string",
          "default": "",
          "description": "Absolute path to the roc compiler used for executable Roc hover, diagnostics, completion, and definition. Empty uses the ROCCI_ROC_PATH environment variable or roc on PATH. Restart the language server after changing this."
        },
        "rocci.preview.rocciPath": {
          "type": "string",
          "default": "",
          "description": "Absolute path to the rocci binary. Empty uses a debug build, a verified GitHub-release extract, or PATH."
        },
        "rocci.preview.rocdownPath": {
          "type": "string",
          "default": "",
          "description": "Absolute path to the rocdown binary. Empty uses a debug build, a verified GitHub-release extract, or PATH."
        },
        "rocci.tools.channel": {
          "type": "string",
          "enum": ["stable", "dev"],
          "enumDescriptions": [
            "Install from the latest GitHub release",
            "Install from the rolling koliyo/rocci tag and release named dev"
          ],
          "default": "stable",
          "description": "GitHub release channel. stable uses /releases/latest. dev uses the rolling tag/release `dev` (assets named rocci-dev-<sha>-<triple>.tar.gz)."
        },
        "rocci.tools.autoUpdate": {
          "type": "boolean",
          "default": true,
          "description": "Check GitHub releases on activate when not debugging."
        },
        "rocci.lsp.verbose": {
          "type": "boolean",
          "default": false,
          "description": "Write verbose Rocci language-server logs (child spawn, projection sync, mapped hover) to the Rocci output channel. Also set when rocci.lsp.trace.server is verbose. Restart is automatic."
        },
        "rocci.lsp.trace.server": {
          "type": "string",
          "enum": [
            "off",
            "messages",
            "verbose"
          ],
          "default": "off",
          "description": "Traces the communication between VS Code and the Rocci language server."
        }
      }
    }
  }
}
